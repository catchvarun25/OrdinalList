import { StatusBar, StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 20,
        backgroundColor: "#000",
    },
    header: {
        fontSize: 13,
        color: "white",
        marginBottom: 10,
    },
    description: {
        fontSize: 16,
        color: "white",
    },
})

export { styles };